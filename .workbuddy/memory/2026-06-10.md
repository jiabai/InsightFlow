# 2026-06-10

## 配置仓库元数据（setup-matt-pocock-skills）
- 使用 `setup-matt-pocock-skills` 完成了仓库元数据配置
- 选项：本地 Markdown issue tracker（.scratch/）、默认 triage 标签、单上下文领域文档
- 创建了 `AGENTS.md`（含 `## Agent skills` 配置块）
- 创建了 `docs/agents/issue-tracker.md`（本地 Markdown issue 追踪规范）
- 创建了 `docs/agents/triage-labels.md`（五标准角色标签映射）
- 创建了 `docs/agents/domain.md`（单上下文领域文档消费规则）

## 代码审查与修复
对 `src/be/` 进行了全面代码审查，修复了以下问题：

### 🔴 严重问题
- 删除重复文件 `fastpai_logger.py`（fastapi_logger.py 的完全重复）
- 删除过时文件 `file_management_service.py`（旧版路由，使用独立 DB/Redis 实例）
- 移除 `config_manager.py` 和 `file_routes.py` 中硬编码的 LLM API Key
- 将 `database_manager.py` 的 DB 连接参数全部改为环境变量（DB_HOST/DB_USER/DB_PASSWORD/DB_NAME）
- 修复 `database_manager.py` 中 `_create_all_tables()` 过早 dispose engine 的 bug

### 🟡 中等问题
- 修复 `question_generator.py` 中 `_label_and_save_questions` 同步调用阻塞事件循环（改为 async）
- 删除 `knowledge_processing_service.py` 中未使用的 `_generate_questions_and_save` 方法
- 清理 `utils.py` 中约 130 行注释掉的旧代码
- 移除 `tag_management.py` 模块级 `setup_logging()` 副作用调用
- 修复 `knowledge_processing_main.py` 缺少 `user_id` 参数的问题

### README
- 更新了 MySQL 连接配置说明，反映环境变量覆盖

## 架构审查（improve-codebase-architecture）
使用 improve-codebase-architecture skill 对全项目进行了系统性架构审查，生成了自包含 HTML 报告。
报告聚焦 6 个重构候选，按推荐强度排序：

### Strong 推荐（4 个）
1. **统一三个日志系统** — fastapi_logger / logger_config / asyncio_logger 各自为政，统一为一个深模块
2. **消除并行 LLM 代码路径** — 路由直接使用 AsyncOpenAI 绕过 LLMClient 和 ai_sdk/LanguageModel，三条路径三种默认模型
3. **折叠三个传递模块** — StorageManager (57行, 1行委托), LLMConfigManager (42行 if-elif), exceptions (11行空类)
4. **替换模块级全局变量为依赖注入** — shared_resources 全局单例造成循环依赖和不可测试性

### Worth exploring（2 个）
5. **引入 DB Repository 接口** — ORM 模型泄漏到所有调用方，无法在内存中测试
6. **按领域拆分 file_routes** — 999 行包含 7 个端点簇 + 工具函数 + 诊断代码

首要建议：消除并行 LLM 代码路径（杠杆最高、风险最低）。

## 执行：候选 4 — 依赖注入重构（A+A+A）
经 grilling 设计对话确认方案（FastAPI Depends + 构造函数注入 + 分别注入），已完成实现：

### 改动文件（4 个）
- **shared_resources.py**：移除模块级 `db_manager=DatabaseManager()` / `redis_manager` / `storage_manager` 全局变量。实例通过 `lifespan` 存入 `app.state`。`background_tasks` 和 `MAX_CONCURRENT_TASKS` 内聚到 file_routes.py。
- **file_routes.py**：依赖函数 `get_db()` / `get_database_manager()` / `get_redis_manager()` / `get_storage_manager()` 改为从 `request.app.state` 获取实例。所有 10 个路由处理器注入 `db_manager=Depends(get_database_manager)`。`background_tasks` 移至本模块。
- **knowledge_processing_service.py**：移除 `from shared_resources import db_manager, redis_manager`，改为构造函数注入 `__init__(..., db_manager, redis_manager)`。
- **knowledge_processing_main.py**：独立创建 DatabaseManager 和 RedisManager 实例，传入 KnowledgeProcessingService。

### 关键成果
- 消除 api_services ↔ llm_knowledge_processing 循环依赖
- 路由可通过 `app.dependency_overrides` 在测试中注入 mock
- 后台服务通过构造函数明确声明依赖
## 执行：候选 6 — 按领域拆分路由
### 改动文件（5 个）
- **新建 question_routes.py**（226行）：generate_questions, run_service, dump_asyncio_tasks, get_questions_by_file
- **新建 llm_routes.py**（318行）：LLMQueryRequest, llm_query, llm_query_stream
- **file_routes.py**：991行 → 559行，仅保留文件上传/下载/元数据/状态 CRUD + filter_html_links
- **api_services_main.py**：导入三个路由

### 架构重组完成汇总
全部 6 个候选方案今晚全部执行完毕：

| 状态 | 候选 | 成果 |
|------|------|------|
| ✅ | 4. DI | 模块级全局 → app.state + Depends |
| ✅ | 3. 折叠传递 | 删 storage_manager(57行) + llm_config_manager(42行) |
| ✅ | 1. 统一日志 | 新建 insight_logger.py，删 3 个旧文件 |
| ✅ | 2. LLM 统一 | 新建 llm_gateway.py，三条路径合一 |
| ✅ | 6. 拆分路由 | 991行 → 3个聚焦模块 |
| ✅ | 5. DB 仓库 | 新建 InsightRepository 协议 + MySQL/InMemory 适配器 |

总计：新建 8 个模块，删除 8 个旧文件，修改 17 个文件。全部 6 个候选完成。

## deepresearch_agent 规范化
对 deepresearch_agent 模块进行独立规范化，为后续与 InsightFlow 衔接做准备：

### 变动（3 个文件）
- **__init__.py**：导出核心 API（generate_plan, execute_research, research_async, web_search, extract_contents）+ 数据模型
- **tools_web_search.py**：引入 ResearchSession 封装搜索去重状态，消除 _SEEN_URLS/_SEEN_QUERIES 全局变量，build_web_search_tool 支持 session 参数，向后兼容
- **research_agent.py**：新增 execute_research_async() 异步入口（asyncio.to_thread），execute_research_agent 支持可选 session 参数

## deepresearch_agent 目录重组（方案A）
- 创建 `providers/` 文件夹（base/zhipu/metaso + __init__.py）
- 重命名 8 个文件：deepresearch_generate→cli, plan_service→plan, tools_web_search→search, resilient_crawl→crawler, system_prompt→prompts, base_search_provider→base.py, zhipu_search_provider→zhipu.py, metaso_search_provider→metaso.py
- 更新所有跨模块 import 引用
- 最终结构：9 个扁平文件 + providers/ 文件夹(4)，语法全过

## deepresearch_agent 状态标记
- 分析 `deepresearch_agent/` 目录去留：与 src/ 主产品零依赖，为独立实验原型
- 更新 `README.md`：目录树标注 `[EXPERIMENTAL]`，补充模块状态说明（免费API/手写agent/中英混杂/无校验/计划用langchain重写）
