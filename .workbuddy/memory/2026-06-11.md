## 2026-06-11

### 架构重构: src/be → src/server, src/fe → src/extension

- 重命名 `src/be/` → `src/server/`, `src/fe/` → `src/extension/`
- 批量更新 28 个 Python 文件中的 `from be.xxx` → `from server.xxx`
- 更新 pytest.ini (pythonpath), .gitignore, README.md 所有引用
- 更新 6 个 tests/extractor/*.cjs 中的 require 路径
- 修复 conftest.py 和 shared_resources.py 中的注释残留
- 最终确认: 项目中不再有任何 `src/be` 或 `src/fe` 的引用

### grill-with-docs 审查: src/extension/

审查发现 7 项矛盾并全部决策:
1. Content vs File 术语 → 统一重命名为 Content (Option A)
2. Content Status 双套状态机 → 分层: 领域层 (Pending/Processing/Completed/Failed) + 流水线子状态 (idle/extracting/uploading/generating)
3. Tag 层级未实现 → CONTEXT.md 标注 [待实现]
4. 双版本内容提取器 → 统一到 ContentExtractor.cjs, 删除 mainContentExtractor.js
5. API 7/13 方法返回 any → 严格类型化
6. MainPage.vue 死代码+500行单体 → 清理+拆出 useContentPanel/useQuestionList
7. 目录平铺 → services/ scripts/ ui/ 三层结构

更新了 CONTEXT.md (移除 Chunk, 更新状态机, 标注 Tag, 统一术语) 和创建 ADR 001

### 执行 ADR 001 重构

完成 7 项重构：
1. **Content 术语统一**: uploadResult.ts, questionTypes.ts, apiService.ts, apiClient.cjs 中所有 file_id → content_id, FileInfo → ContentHistoryItem, pollFileStatus → pollContentStatus, getUserFiles → getUserContents
2. **双层层状态机**: statusTracker.cjs 新增 PIPELINE_TO_DOMAIN 映射和 domainStatus getter
3. **内容提取器统一**: ContentExtractor.cjs 新增 getIframeDocument() iframe 支持，删除 extractors/mainContentExtractor.js
4. **API 严格类型化**: 新建 lib/apiTypes.ts (UploadResponse, ContentStatusResponse, ContentHistoryItem, StreamChunk)
5. **MainPage.vue 重构**: 修复双 onMounted bug + 清理孤立代码，拆出 hooks/composables/useContentPanel.ts 和 useQuestionList.ts，组件从 472 行降到 135 行
6. **函数重命名**: simulateAPICall → executeAPICall (消除误导)
7. **目录整理**: 删除 extractors/, 新增 hooks/composables/, 保留 WXT 框架约定
8. 更新 ADR 001 以反映 WXT 框架约束

### Clearly-Reader 功能文档

- 深入分析 Clearly-Reader Chrome 扩展（Manifest V3, v3.0.4）的完整功能
- 创建 docs/clearly-reader/README.md — 含 10 大功能模块的详细中文文档
  - 核心阅读（内容提取算法、40+ 站点专用规则）
  - 外观定制（6 种主题、6 种字体、图书布局、排版参数）
  - 文本交互（选中弹窗、翻译 100+ 语言、大纲目录）
  - TTS 朗读（浏览器引擎 + Microsoft AI 引擎）
  - 导出分享（PDF/Docx/Markdown/EPUB/MP3/Kindle）
  - CLIP 云服务（剪藏/RSS/AI 摘要/播客）
  - 键盘快捷键、用户系统、配置管理、辅助功能
  - 技术架构图、文件结构速查、与 InsightFlow 的关系说明
- 遵循项目规范: 使用 CONTEXT.md 的领域语言（Content/Session），文档放在 docs/ 下
